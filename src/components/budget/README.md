# budget

## 目录职责
- 预算组件目录，负责预算配置与预算状态展示。
- 页面与容器组件通常直接组合本目录能力。
- 路径：`src/components/budget/`

## 文件说明
- `budget-form.tsx`：表单文件，处理输入校验与提交流程。
- `card.tsx`：card 组件文件，承载界面展示与交互逻辑。
- `detail-form.tsx`：表单文件，处理输入校验与提交流程。
- `detail.tsx`：detail 组件文件，承载界面展示与交互逻辑。
- `form.tsx`：表单文件，处理输入校验与提交流程。
- `index.tsx`：模块入口文件，负责对外导出公共能力。
- `list-form.tsx`：表单文件，处理输入校验与提交流程。
- `list.tsx`：list 组件文件，承载界面展示与交互逻辑。
- `type.ts`：类型定义文件，约束模块输入输出数据结构。
- `use-budget-detail.ts`：use budget detail 逻辑文件，封装该模块通用处理逻辑。
- `util.ts`：util 逻辑文件，封装该模块通用处理逻辑。

## 子目录说明
- （暂无一级子目录）

## 维护约束
- 保持文档紧凑：优先写职责、调用关系和边界，不贴实现细节。
- 新增、删除、重命名文件或目录后，必须同步更新本 README。
- 接口、依赖、数据流变化时，补充影响面说明并更新上层目录 README。
