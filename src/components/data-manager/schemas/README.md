# schemas

## 目录职责
- Schema 目录，维护数据结构约束定义。
- 数据管理流程会引用本目录规则，约束导入数据字段结构。
- 路径：`src/components/data-manager/schemas/`

## 文件说明
- `alipay.js`：支付宝账单数据结构定义，用于导入时字段解析与映射。
- `wechat.js`：微信账单数据结构定义，用于导入时字段解析与映射。

## 子目录说明
- （暂无一级子目录）

## 维护约束
- 保持文档紧凑：优先写职责、调用关系和边界，不贴实现细节。
- 新增、删除、重命名文件或目录后，必须同步更新本 README。
- 接口、依赖、数据流变化时，补充影响面说明并更新上层目录 README。
