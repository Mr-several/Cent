# tidal

## 目录职责
- 同步核心目录，承载多端同步适配与关键同步流程。
- 该目录位于核心链路，变更需关注兼容性与数据一致性。
- 路径：`src/tidal/`

## 文件说明
- `gitee.ts`：Gitee 同步实现，封装 Gitee 侧文件读写与同步流程。
- `github.ts`：GitHub 同步实现，封装 GitHub 侧文件读写与同步流程。
- `index.ts`：模块入口文件，负责对外导出公共能力。
- `s3.ts`：S3 同步实现，封装对象存储的上传下载与同步逻辑。
- `web-dav.ts`：WebDAV 同步实现，封装 WebDAV 读写与同步流程。

## 子目录说明
- （暂无一级子目录）

## 维护约束
- 保持文档紧凑：优先写职责、调用关系和边界，不贴实现细节。
- 新增、删除、重命名文件或目录后，必须同步更新本 README。
- 接口、依赖、数据流变化时，补充影响面说明并更新上层目录 README。
