# currency

## 目录职责
- 币种与汇率能力目录，提供货币数据加载与转换基础。
- 上层通常通过统一 API 调用该目录能力。
- 路径：`src/api/currency/`

## 文件说明
- `currencies.ts`：currencies 逻辑文件，封装该模块通用处理逻辑。
- `data.json`：data 数据文件，提供静态配置或常量数据源。
- `index.ts`：模块入口文件，负责对外导出公共能力。
- `kurrency.ts`：kurrency 逻辑文件，封装该模块通用处理逻辑。

## 子目录说明
- （暂无一级子目录）

## 维护约束
- 保持文档紧凑：优先写职责、调用关系和边界，不贴实现细节。
- 新增、删除、重命名文件或目录后，必须同步更新本 README。
- 接口、依赖、数据流变化时，补充影响面说明并更新上层目录 README。
